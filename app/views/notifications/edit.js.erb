$("#modal_html" )[0].innerHTML="<%= escape_javascript(render 'form') %>";
$('#editnotification').modal('show');
$('#example-multiple-selected').multiselect();
$('#notification_designation_ids').multiselect();
function ValidateFileUpload() {
	var fuData = document.getElementById('notification_attachment');
	var FileUploadPath = fuData.value;

    var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
    if (Extension == "" ||Extension == "doc" || Extension == "docx" || Extension == "msg" || Extension == "gif" || Extension == "png" || Extension == "bmp" || Extension == "jpeg" || Extension == "jpg" || Extension == "xcf" || Extension == "gif" || Extension == "TIFF" || Extension == "GIF" || Extension == "odt" || Extension == "pages" || Extension == "tar" || Extension == "pdf" || Extension == "vcf" || Extension == "csv" || Extension == "indd") {
            if (fuData.files && fuData.files[0]) {

                var size = fuData.files[0].size;

                if(size > MAX_SIZE){
                    alert("Maximum file size exceeds");
                    return;
                }else{
                    var reader = new FileReader();

                    reader.onload = function(e) {
                        $('.errorr').attr('src', e.target.result);
                    }

                    reader.readAsDataURL(fuData.files[0]);
                }
            }
    } 
else {
        show_error("Attachment only allows file types of GIF, PNG, JPG, JPEG, BMP, doc, docx, msg, odt, pages, tar, pdf, vcf, csv, and indd ");
        return false;
    }
 
 return true
}